package com.vitalii.komaniak.hacaton_app.presentation.components.cards

import androidx.compose.runtime.Composable

class CardsProvider {

    @Composable
    fun renderCardByType(cardType: String) {
        when (cardType) {

        }
    }
}